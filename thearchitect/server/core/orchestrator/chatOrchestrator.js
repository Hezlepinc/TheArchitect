export async function chatOrchestrator(message, config) {
  // Placeholder for AI logic
  // Later: connect to OpenAI, Anthropic, etc.
  return `Hello from ${config.assistantName}! You said: "${message}"`;
}
